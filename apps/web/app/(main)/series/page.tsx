import { PageContainer } from "@/components/layout/PageContainer";
import { ScreenHeader } from "@/components/layout/ScreenHeader";

export default function SeriesPage() {
  return (
    <PageContainer>
      <ScreenHeader
        title="Séries"
        description="Suas séries, animes e doramas vão aparecer aqui."
      />
      {/* Estrutura preparada para a listagem de séries — TASK futura. */}
    </PageContainer>
  );
}
