# supabase/

Pasta preparada para receber as migrations quando o banco for
retomado. **Intencionalmente vazia de schema nesta fase** — TASK-001
foi explícito: "não crie banco de dados". Só o `config.toml` mínimo do
Supabase CLI está aqui, para a pasta existir com a estrutura certa.
