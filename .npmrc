# node-linker, shamefully-hoist e strict-peer-dependencies moraram
# aqui antes, mas pnpm 10.33+ só le auth/registry de .npmrc — todo o
# resto precisa estar em pnpm-workspace.yaml (nodeLinker,
# shamefullyHoist, strictPeerDependencies). Ver esse arquivo.
